package com.crmSDETCalenderDynamicPath;

import org.testng.annotations.Test;

public class HandlingCalender {
	@Test 
	
	public void handleCalender ()   {
	
		/* String update = month+" "+year;
		 System.out.println(updatedmonth);
		 System.out.println(update);  */
		
		
		/*WebDriver driver = new ChromeDriver ();
		driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(15,TimeUnit.SECONDS);
		driver.get("https://www.makemytrip.com");
		
		Actions act = new Actions (driver);
		act.moveByOffset(50, 100).click().perform();
		driver.findElement(By.xpath("//p[@data-cy='departureDate']")).click();
		String path="//div[text()='November 2021']/ancestor::div[@class='DayPicker-Month']/descendant::p[text()='26']";
		driver.findElement(By.xpath(path));
		*/
		
		
	}
}
